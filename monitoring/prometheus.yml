global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "npc-dialogue-engine"
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
        labels:
          service: "npc-dialogue"
          environment: "development"
